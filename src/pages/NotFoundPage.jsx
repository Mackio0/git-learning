
const NotFoundPage = () => {
  return (
    <div>NotFoundPage his part attempts to list the contents of the directory /fake/directory.
However, since /fake/directory is likely a non-existent directory, this command will result in an error message.</div>
  )
}

export default NotFoundPage